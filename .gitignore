# 构建产物
/build/
/cmake-build-*/
compile_commands.json

# 根目录误放的构建产物（若 CMake 配置把二进制吐到仓库根）
/seckill-cpp

# 运行期生成目录
/uploads/
/logs/

# 本地配置覆盖（含密码，勿提交）
config.local.json

# JMeter 压测副产物统一收口到 jmeter/out/（保留 jmeter/*.jmx 与 scripts/jmeter-baseline.sh 为源码）
jmeter/out/
# JMeter 安装包（勿提交，体积大且含二进制；置于仓库任意目录均忽略）
apache-jmeter-*.tgz
apache-jmeter-*.tgz.sha512

# 编辑器/工具
.*/
*.swp
